@model IEnumerable<baikiemtra.Models.NhanVien>

@{
    ViewBag.Title = "Index";
}

<h2>Danh sách nhân viên</h2>

<p>
    @Html.ActionLink("Thêm mới", "Creat")'
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaNhanVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoTen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiaChi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaNhanVien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoTen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DiaChi)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new {MaNhanVien=item.MaNhanVien }) |
            @Html.ActionLink("Chi tiết", "Details", new { MaNhanVien = item.MaNhanVien }) |
            @Html.ActionLink("Xóa", "Delete", new { MaNhanVien = item.MaNhanVien })
        </td>
    </tr>
}

</table>
